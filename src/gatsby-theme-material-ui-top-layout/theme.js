import { createTheme } from '@material-ui/core';
const theme = createTheme({
  palette: {
    primary: {
      main: "#e38700",
      light: "#FFF8E5",
    },
    secondary: {
      main: "#FFB344",
      light: "#CCF6C8",
    },
    black: "#2B2B2B",
    white: "#F0F0F0"
  },
});

export default theme;
